name: tests
run-name: tests
on: [push]
jobs:
  production-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose -f prod.docker-compose.yml up --build
      - run: docker container logs notebook
      